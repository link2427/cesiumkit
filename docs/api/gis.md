# GIS Integration

GeoPandas and pandas DataFrame helpers. These functions let you convert a
`GeoDataFrame` or a plain `DataFrame` into cesiumkit `Entity` objects with a
single call.

Install the optional dependencies with:

```bash
pip install cesiumkit[gis]
```

::: cesiumkit.gis
