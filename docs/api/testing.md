# Headless Testing

Playwright-based helpers for rendering and screenshotting viewers without a
display. Used by the CI `render-check` job and available for local
verification (`pip install "cesiumkit[testing]"`).

::: cesiumkit.testing
